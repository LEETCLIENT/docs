# timer

## reset()
Сбрасывает таймер до текущего времени
## getTime(): number
Возвращает сколько миллисекунд прошло между reset() и текущим моментом времени
## reached(time): boolean
Проверяет, прошло ли с последнего вызова reset(), `time` миллисекунд.