# Свап в хотбаре

```lua
module.set("Silent swap", 0)
function enabled()
    local set_slot = internal.new_instance(network.c2s("set_carried_item"), 0)
    network.send_packet(set_slot)
    client.chat("slot silent swapped to 0")
end
```